[
  {
    "route": "GET /api/stripe/config",
    "cases": [
      { "as": "anon", "status": 401, "note": "server.js mounts routes/stripe.js only when Stripe keys are set; the harness has none, so these specs pin the unmounted behaviour. A Stripe test-mode tier comes with the checkout fix (owner decision E6)." },
      { "as": "user", "status": 404, "note": "not mounted in the harness" }
    ]
  },
  {
    "route": "POST /api/stripe/create-subscription",
    "cases": [
      { "as": "anon", "status": 401 },
      { "as": "user", "status": 404, "note": "not mounted in the harness; never called authorised (creates Stripe objects)" }
    ]
  },
  {
    "route": "POST /api/stripe/start-free-trial",
    "cases": [
      { "as": "anon", "status": 401 },
      { "as": "nosub", "status": 404, "note": "not mounted in the harness: the trial page's only action does not exist without Stripe keys (the Free trial row)" }
    ]
  },
  {
    "route": "POST /api/stripe/validate-discount",
    "cases": [
      { "as": "anon", "status": 401 },
      { "as": "user", "status": 404, "note": "not mounted in the harness" }
    ]
  },
  {
    "route": "POST /api/stripe/webhook",
    "cases": [
      { "as": "anon", "status": 401, "note": "mounted after the /api sign-in gate, so Stripe itself would get 401 here too - the paid-checkout bug (owner decision E6)" }
    ]
  }
]
