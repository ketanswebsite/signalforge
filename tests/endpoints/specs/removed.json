[
 {
  "route": "POST /api/signals/from-scan",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed before 5ef1168: the 7 AM scan stores its signals in process (lib/scanner/signal-store.js). It used to answer anonymously in front of the sign-in gate; now an anonymous POST stops at the gate. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/debug/all-trades",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): always failed. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/test-admin",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/user-analytics",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/debug/users",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): returned every user's email. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/recover-users",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): wrote users through a GET. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/signals/add-to-portfolio/:signalId",
  "absent": true,
  "path": "/api/signals/add-to-portfolio/{pendingSignalId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): changed a signal for every portfolio. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/signals/dismiss/:signalId",
  "absent": true,
  "path": "/api/signals/dismiss/{pendingSignalId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): changed a signal for every portfolio. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/executor/manual-execute/:market",
  "absent": true,
  "path": "/api/executor/manual-execute/UK",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): fired the 1 PM executor for any signed-in account. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/executor/logs",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/exit-monitor/check-exits",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran the all-users exit pass. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/exit-monitor/check-trade/:tradeId",
  "absent": true,
  "path": "/api/exit-monitor/check-trade/{userActiveTradeId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran the exit check for any trade. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/run-migration-trade-alerts",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran DDL that boot already runs. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/execute-signals/:market",
  "absent": true,
  "path": "/api/execute-signals/UK",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): an admin-checked duplicate of POST /api/admin/test-execution. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/force-cron-trigger",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): an admin-checked duplicate of POST /api/admin/test-scan. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/admin/remove-oauth-user",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the Users tab deletes accounts through the admin router. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/stats",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the dashboard reads the admin router. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/migrate-capital",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): a finished one-time migration to per-user capital. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/fix-position-count",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): an India-only counter fix, superseded by POST /api/ops/reconcile-capital. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/dismiss-old-signals",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller, and it crashed on a missing body or a bad date. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/scanner/cron-status",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; /health reports the cron state. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/scanner/status",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/health/trading-automation",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the executor logs are in the admin API. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /auth/debug",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 302,
    "check": "redirectLogin",
    "note": "removed in I5 (\"Remove 14 dead routes\"): it showed anyone the OAuth configuration. Not under /api: the page gate sends an anonymous caller to /login."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/portfolio/initialize-capital",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; capital rows are created on first use, and it crashed on a non-numeric amount. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/portfolio/check-exits",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the exit monitor is the only closer. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/market-cap/:symbol",
  "path": "/api/market-cap/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; market caps travel with the scan signals. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/market-cap/bulk",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/user/location",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): its only caller, public/js/pricing.js, was loaded by no page; geoip-lite goes with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/ml/analysis/:symbol",
  "path": "/api/ml/analysis/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/risk-params",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/detect-patterns",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/ml/sentiment/:symbol",
  "path": "/api/ml/sentiment/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/portfolio-risk",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/alerts",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/train",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/database",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned tests/database.test.js against the live database (a user_settings row per click and a TEST.NS signal) and always failed. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/performance",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned tests/performance.test.js against the live database. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/verify-system",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned scripts/verify-system.js, which always stopped at its file check (public/js/trades-ui.js never existed). Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/test",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): its only caller was scripts/health-check.js, removed with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/auth/token",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): issued an admin JWT that nothing used; the admin API is session-only. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/auth/verify",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): checked an admin JWT that nothing used, and crashed with 500 because cookie-parser was never installed. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/events",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the admin SSE stream, which nothing ever published to; the dashboard no longer opens it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/subscriptions/:id/extend",
  "path": "/api/admin/subscriptions/{victimSubscriptionId}/extend",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): no page called it (the Subscriptions tab only cancels). Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/unified",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the audit log viewer; its only caller, public/js/admin-audit.js, was loaded by no page and goes too. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/analytics",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the audit log viewer; its only caller, public/js/admin-audit.js, was loaded by no page and goes too. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/export",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the audit log viewer; its only caller, public/js/admin-audit.js, was loaded by no page and goes too. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/statistics",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): no caller; it summarised the admin activity log, which nothing writes. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/:id",
  "path": "/api/admin/audit/1",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the audit log viewer; its only caller, public/js/admin-audit.js, was loaded by no page and goes too. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/audit/:id/export",
  "path": "/api/admin/audit/1/export",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): the audit log viewer; its only caller, public/js/admin-audit.js, was loaded by no page and goes too. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/analytics/overview",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): a placeholder ('Analytics coming soon') that no page called. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/database/health",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): no caller; the Database tab reads /api/admin/system/health. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/database/status",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): a legacy duplicate that no page called and that failed with 500 on every call. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/database/maintenance/analyze-table",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): it put tableName into the SQL unvalidated, and the page method that called it was never invoked. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): a legacy summary that no page called (the Settings tab reads /api/admin/settings/*). Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/system/trigger-scan",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): a stub that triggered nothing yet answered 'Stock scanner triggered'. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/sse/connections",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): it listed connections to the removed SSE stream; no caller. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/stripe/validate-discount",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I6 (\"Remove dead admin routes, JWT and SSE\"): a stub that rejected every code; its only caller, public/js/checkout.js, is loaded by no page. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler (the harness mounts no Stripe router either), the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/stripe/start-free-trial",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I10 (\"Free-trial lifecycle\"): the trial page starts trials with POST /api/user/subscription/start-trial, which is always mounted and allows one free trial per account. This duplicate existed only with Stripe keys, never called Stripe, took any plan code and allowed unlimited re-trials. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "nosub",
    "status": 404,
    "note": "signed in: no handler (and routes/stripe.js is not mounted in the harness), the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/analytics/reports",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that answered \"Report generation started\" and generated nothing; the Generate Report button and its dialog (which never opened: the page has no modal container) went with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/database/migrations/run",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that ran nothing and answered success, so the Database tab said \"Migrations completed successfully\"; its button went with it. Migrations are applied by hand. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/database/migrations/run-single",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that answered success for any file name, even one not in migrations/ (the pinned bug); the per-file Run buttons went with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/database/backups",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that always listed no backups beside a notice claiming daily automatic backups; the Backups sub-tab went with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/database/backups/create",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that made no backup and answered \"Backup created\" with a file name. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/database/backups/download/:filename",
  "path": "/api/admin/database/backups/download/harness-no-such-backup.sql",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that answered 200 \"coming soon\" for any file name (the pinned bug); no backup ever existed. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/database/backups/restore",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that answered success for any file name (the pinned bug), so the Database tab said \"Backup restored successfully\". Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings/payment",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): it offered PayPal and Razorpay, which the app has no code for, beside masked Stripe keys in inputs that saved nothing; GET /api/admin/settings/general says which Stripe keys are set. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings/email-templates",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a hard-coded list of email templates and SMTP settings; the app sends no email. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings/email-templates/:templateId",
  "path": "/api/admin/settings/email-templates/welcome",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that answered a sample template for any id (the pinned bug); the app sends no email. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "PUT /api/admin/settings/email-templates/:templateId",
  "path": "/api/admin/settings/email-templates/welcome",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that saved nothing and answered \"Template updated successfully\". Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings/feature-flags",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): eight hard-coded flags that nothing in the app reads. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/settings/feature-flags",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that saved nothing and answered \"Feature flags updated successfully\". Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/settings/broadcast",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that sent nothing and reported a made-up count (\"Broadcast sent to 5 users\"); the Settings tab now sends a web push broadcast through POST /api/admin/push/broadcast. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/settings/maintenance",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): it read MAINTENANCE_MODE, which nothing else reads: the app has no maintenance mode. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/settings/maintenance-mode",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I12b (\"Admin portal honesty\"): a placeholder that switched nothing and answered \"Maintenance mode enabled\". Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 }
]
