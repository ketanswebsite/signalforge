[
 {
  "route": "POST /api/signals/from-scan",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed before 5ef1168: the 7 AM scan stores its signals in process (lib/scanner/signal-store.js). It used to answer anonymously in front of the sign-in gate; now an anonymous POST stops at the gate. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/debug/all-trades",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): always failed. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/test-admin",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/user-analytics",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/debug/users",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): returned every user's email. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/recover-users",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): wrote users through a GET. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/signals/add-to-portfolio/:signalId",
  "absent": true,
  "path": "/api/signals/add-to-portfolio/{pendingSignalId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): changed a signal for every portfolio. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/signals/dismiss/:signalId",
  "absent": true,
  "path": "/api/signals/dismiss/{pendingSignalId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): changed a signal for every portfolio. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/executor/manual-execute/:market",
  "absent": true,
  "path": "/api/executor/manual-execute/UK",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): fired the 1 PM executor for any signed-in account. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "GET /api/executor/logs",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): exposed platform data. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/exit-monitor/check-exits",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran the all-users exit pass. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/exit-monitor/check-trade/:tradeId",
  "absent": true,
  "path": "/api/exit-monitor/check-trade/{userActiveTradeId}",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran the exit check for any trade. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/run-migration-trade-alerts",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): ran DDL that boot already runs. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/execute-signals/:market",
  "absent": true,
  "path": "/api/execute-signals/UK",
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): an admin-checked duplicate of POST /api/admin/test-execution. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/force-cron-trigger",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in 5ef1168 (\"Close the any-account holes\"): an admin-checked duplicate of POST /api/admin/test-scan. Anonymous: 401 at the /api sign-in gate (server.js:885)."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers (server.js:3914)"
   }
  ]
 },
 {
  "route": "POST /api/admin/remove-oauth-user",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the Users tab deletes accounts through the admin router. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/admin/stats",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the dashboard reads the admin router. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/migrate-capital",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): a finished one-time migration to per-user capital. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/fix-position-count",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): an India-only counter fix, superseded by POST /api/ops/reconcile-capital. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/dismiss-old-signals",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller, and it crashed on a missing body or a bad date. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/scanner/cron-status",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; /health reports the cron state. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/scanner/status",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/health/trading-automation",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the executor logs are in the admin API. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /auth/debug",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 302,
    "check": "redirectLogin",
    "note": "removed in I5 (\"Remove 14 dead routes\"): it showed anyone the OAuth configuration. Not under /api: the page gate sends an anonymous caller to /login."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/portfolio/initialize-capital",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; capital rows are created on first use, and it crashed on a non-numeric amount. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/portfolio/check-exits",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; the exit monitor is the only closer. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/market-cap/:symbol",
  "path": "/api/market-cap/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller; market caps travel with the scan signals. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/market-cap/bulk",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): no caller. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/user/location",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I5 (\"Remove 14 dead routes\"): its only caller, public/js/pricing.js, was loaded by no page; geoip-lite goes with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/ml/analysis/:symbol",
  "path": "/api/ml/analysis/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/risk-params",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/detect-patterns",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/ml/sentiment/:symbol",
  "path": "/api/ml/sentiment/HARNESS.L",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/portfolio-risk",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/alerts",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/ml/train",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I14 (\"Remove the legacy ML toolkit\"): nothing called it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: the /api/ml router now holds only the conviction routes; the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/database",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned tests/database.test.js against the live database (a user_settings row per click and a TEST.NS signal) and always failed. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/performance",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned tests/performance.test.js against the live database. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "POST /api/admin/tests/verify-system",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): it spawned scripts/verify-system.js, which always stopped at its file check (public/js/trades-ui.js never existed). Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "user",
    "status": 403,
    "note": "not an admin: 403 at the /api/admin guard"
   },
   {
    "as": "admin",
    "status": 404,
    "note": "admin: no handler left in the admin router or server.js, the 404 fallback answers"
   }
  ]
 },
 {
  "route": "GET /api/test",
  "absent": true,
  "cases": [
   {
    "as": "anon",
    "status": 401,
    "note": "removed in I13 (\"Remove the admin Run-tests feature\"): its only caller was scripts/health-check.js, removed with it. Anonymous: 401 at the /api sign-in gate."
   },
   {
    "as": "token",
    "status": 401,
    "note": "the ops token does not reopen it"
   },
   {
    "as": "user",
    "status": 404,
    "note": "signed in: no handler, the 404 fallback answers"
   }
  ]
 }
]
